image:  epitechcontent/epitest-docker

stages:
    - build
    - push

job:build:
    stage: build
    script:
        - make fclean
        - make re
        - make

job:push:
  stage: push
  script:
    - make fclean
    - git remote
  when: on_success
